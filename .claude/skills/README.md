# Claude skill discovery

The canonical instructions live in [`../../skills`](../../skills). Keep one source of truth: install this repository as a Claude plugin and let the plugin manifest expose that directory.
